/**
 * The benchmark's methodology copy, generated from the vanilla page so all four
 * builds say the same thing. Only the clauses that describe what is actually
 * measured differ, because these ports measure the Vue wrapper rather
 * than the engine on its own.
 *
 * Regenerate with scratchpad/gen-method.mjs after editing benchmark.html.
 */
export const METHOD_HTML = "<ol>\n  <li>\n    <strong>Refresh-rate calibration.</strong> Before anything runs, the harness samples idle\n    <code>requestAnimationFrame</code> intervals and takes the median to derive your display's\n    refresh rate. That sets the frame budget behind the jank and FPS columns, so on a\n    120&nbsp;Hz panel the budget works out at 8.3&nbsp;ms rather than 16.7&nbsp;ms.\n  </li>\n  <li>\n    <strong>Warm-up, discarded.</strong> Each case gets a short scroll burst whose frames are\n    thrown away, so JIT warm-up and first-paint costs never land in the reported numbers.\n  </li>\n  <li>\n    <strong>Mount.</strong> Constructs a fresh component, renders the first viewport and forces a layout flush. This includes Vue&rsquo;s own mount work, which is the honest figure for a Vue user. Repeated <em>n</em> times; the <em>median</em> is\n    reported, which is far more stable than a mean under background noise.\n  </li>\n  <li>\n    <strong>Sustained scroll.</strong> A rAF loop applies a fixed pixel delta through the engine and calls the wrapper's <code>render()</code>. Because the Vue binding commits row content through a synchronous <code>render(vnode, el)</code>, that work happens within the timed section rather than escaping to a later frame. Two things are recorded per frame: the\n    <em>frame interval</em>, the wall-clock gap between frames, which carries browser layout,\n    paint and compositing with it; and the <em>JS work</em> Vue and the engine spent together. FPS comes\n    from the intervals, while frame p50/p95 report the library's own cost.\n  </li>\n  <li>\n    <strong>Random access.</strong> Jumps to uniformly random indices across the whole dataset\n    via <code>jumpToElement()</code>, repaints, and forces layout. This is the \"seek to row\n    7,431,902\" case, and it is where true virtualization parts company with DOM clustering.\n  </li>\n  <li>\n    <strong>DOM footprint.</strong> Counts every element resident inside the scroll surface at\n    steady state. This is the O(1) claim in its most falsifiable form: the count must not grow\n    with the dataset.\n  </li>\n  <li>\n    <strong>Row complexity.</strong> The complexity sweep pins the dataset size and walks the\n    row template instead. There is a plain card, a twelve-column grid row with a sparkline, a\n    media card with an aspect-ratio thumbnail, star rating and avatar stack, an inline\n    <code>&lt;svg&gt;</code> chart built from gridlines, bars, an area path and a polyline, a\n    row of native form controls, a sixteen-level-deep tree, and a panel carrying gradients,\n    drop shadows and a transform. Every one of them is built from local DOM only, with no\n    network images and no remote fonts, so a run stays deterministic and works offline. The\n    node count on each chip is measured rather than asserted: five rows go into a detached,\n    off-screen probe and the elements are counted.\n  </li>\n  <li>\n    <strong>The control group.</strong> With the baseline enabled, every dataset size is\n    also measured against the same rows rendered with a plain <code>v-for</code> and no virtualiser, which is what you get without one. It renders the identical row template through Vue itself, so it is a fair control rather than a strawman. Two safety\n    limits apply, and both are reported as results rather than hidden: a size needing more\n    than two million DOM nodes is refused outright, and a build that has not finished\n    within ten seconds is abandoned with the row count it reached. One caveat matters for\n    reading the numbers: per-frame JS <em>work</em> is not comparable between the two,\n    because the unvirtualized list does almost nothing in JavaScript per frame while the browser\n    pays the cost at layout and paint. The comparison table therefore comes down to\n    wall-clock frame interval, mount time, and resident DOM.\n  </li>\n  <li>\n    <strong>Engine cost against renderer cost.</strong> Renderer invocations are counted during the measured scroll. <em>Renders&nbsp;/&nbsp;frame</em> is how many rows genuinely\n    entered the viewport on that frame, and it should stay in the low single digits no matter\n    how large the dataset gets. <em>Per row</em> divides the total per-frame JS by those\n    invocations to give the average cost of building one row. Read together, they tell you\n    which side of the boundary your frame budget is going to: the virtualiser, or your own\n    row template.\n  </li>\n</ol>\n<div class=\"note\">\n  <strong>Honest caveats.</strong> FPS is capped by your display. A 60&nbsp;Hz panel cannot\n  report much beyond 60, so <em>frame p50/p95</em>, the library's own JS cost, is the number\n  that actually measures headroom. Heap readings come from <code>performance.memory</code>,\n  which is Chromium-only, quantised, and at the mercy of garbage collection timing, so treat it\n  as an order-of-magnitude signal rather than a precise figure. Sub-millisecond results sit\n  close to the floor of the clock itself, since browsers quantise\n  <code>performance.now()</code> to roughly 100&nbsp;µs; a 0.30&nbsp;ms frame and a\n  0.35&nbsp;ms frame are not meaningfully different, so raise the fling velocity or reach for a\n  heavier row template when you want separation. Numbers also move with background tabs,\n  battery saver, thermal state and extensions, so close the noisy ones before quoting\n  anything. And remember this measures the wrapper together with whatever row template you point it at: a heavier row template lifts every column, which is exactly why the template is\n  a knob you can turn.\n</div>";
